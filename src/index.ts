/**
 * Host loader entry for the browser-only timeline plugin.
 *
 * The timeline rail is a pure presentation feature: it reads the live
 * ConversationSnapshot through the client slot system and renders inside the
 * composer dock. It has no host-side behavior.
 */
export function apply(): void {}
